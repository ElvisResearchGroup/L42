reuse [L42.is/AdamsTowel]
//Matrix: near 4, near 8 iterators given a point?
//operations rolling around?
//---
//What this meant in day 14? "no ambiguity if the next is a local var dec??"
//--
//if b0==0 ( that.pad() return res )
//    return (res*16I)+\vals(that)
//it is parsed as if b0==0(that.pad() return res) return (res*16I)+\vals(that)
//is this ok?
//--
//  /*BUG*/Debug(Map.Coord(row=3I,col=2I))//this prints Map.Coord.POUNDkinvariant() in the stack trace
//--
//Set does not have a (void) mapping mehtod?? checked; still missing.
//---
//sort, maxindex,popmax etc..
Map=Collection.matrix(S.List,row=1I,col=1I)
A={interface [HasToS] class method This (S foo)}
B=Data:{[A] S foo}
Main = (
  A a= B(foo=S"")
  Debug(a)
  m=Map(\[S"hi"])
  ///*BUG*/Debug(Map.Coord(row=3I,col=2I))//this prints Map.Coord.POUNDkinvariant() in the stack trace
  Debug(m)
  )