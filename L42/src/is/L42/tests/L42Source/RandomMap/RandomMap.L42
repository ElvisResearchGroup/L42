Size side
mut Random r
Size waterLevel
Size treeLevel
Size rockLevel
Size maxZ
mut Points montainsTop
DeptMap=MapList.expansion(Size.List,base='zero())
BoolMap=MapList.expansion(Bool.List,base='false())
mut DeptMap map
class method mut DeptMap #default#map(Size side,mut Random r)=
  \(side=side, r=r,maxSeeds=\"-1")
mut DeptMap riverMap
mut BoolMap grassMap
mut BoolMap treeMap
mut BoolMap rocksMap

class method mut Points montainsTop(DeptMap map)=(
  s=map.side()
  low=s/6Size
  mid=s/2Size
  high=5Size*(s/6Size)
  p1=map.rCoord(minX=low,maxX=mid,minY=low,maxY=mid)
  p2=map.rCoord(minX=mid,maxX=high,minY=mid,maxY=high)///TODO: HERE
  p3=map.coord((c.x(p1)+c.x(p1)+c.x(p2))/3,(c.y(p1)+c.y(p1)+c.y(p2))/3);
  p4=map.coord((c.x(p1)+c.x(p2)+c.x(p2))/3,(c.y(p1)+c.y(p2)+c.y(p2))/3);
  p5=map.rCoord()
  p6=map.rCoord()
  p7=map.rCoord()
  
  )

class method mut This(
  Size side,mut Random r,Size waterLevel,Size treeLevel,Size rockLevel,Size maxZ)=(
  

  This(side=side,r=r,
    waterLevel=waterLevel,treeLevel=treeLevel,rockLevel=rockLevel,
    maxZ=maxZ,
mut Points montainsTop
DeptMap=MapList.expansion(Size.List,base='zero())
BoolMap=MapList.expansion(Bool.List,base='false())
  map=DeptMap(side=side, r=r,maxSeeds=\"-1")
mut DeptMap riverMap
mut BoolMap grassMap
mut BoolMap treeMap
mut BoolMap rocksMap

)

method S msg(Size side)=(
  
  g=S"Ground"
  col=S"%g;%g;%g;%g;%g"
  mapMsg=S"""%
  |%col
  |%col
  |%col
  |%col
  |%col
  """
)

