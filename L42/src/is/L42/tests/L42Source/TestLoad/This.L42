reuse[AdamTowel]

Base=Trait:{
  reuse[AdamTowel]
  Main={class method S hello(S that)=S"hello %that"}
  }
Deployed0=Load:Deploy.module(Base.code(),n='Main)<:Trait.code()
Deployed1=Deploy.module(
  url='MiniLib
  code=Base.code()
  name='Main
  )
Deployed2=Load:{reuse[#$MiniLib]}

Test1=(
  {}:Test"BaseLocal"(
    expected=S"hello world"
    actual=Deployed0.hello(S"world")
    )
  {}:Test"BaseRemote"(
    expected=S"hello L42"
    actual=Deployed2.hello(S"L42")
    )
  )
  
  /*
TODO:
- remove unused code in load
- load must throw error if the map can not be completed
- organize must be buggy, there is no way the internal Public is recognized
as Public for the outer organize??
  
Then caching top level

Then IDE
*/