{reuse L42.is/AdamTowel02

Main:{
  Assert.Pre[
    1Size==1Size;
    2Size==2Size
    ]
  (
  Assert.Pre[1Size!=1Size]
  catch error Assert.Bug Debug(S"pass")
  error S"FAIL"
  )
  return ExitCode.normal()
  }


//discuss: is != to be desugared as ! == ?
}