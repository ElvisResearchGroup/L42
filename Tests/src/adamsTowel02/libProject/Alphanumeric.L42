{
ParseFail: Message.$<><{implements Guard.ParseFail}
Fail: Message.$<><{implements Guard}
  
Trait:Resource<><{implements Concept.Base
  S that

  class method
  This #from(S.SBuilder builder)
    This.parse(S.#from(builder:builder))

  class method
  mut S.SBuilder #builder()
    S.SBuilder.empty()

  class method
  This parse(S that)
    
  method toS() this.that()  // Assumes simple, non-destructive parsing
  
  read method
  Bool equals(read This that)
    this.that()==that.that()
    }

  class method
  Library <><(Library right)
  exception Fail{
    return K<><Use.Override[UnsafeRefactor.MiniDataTrait();Trait()]<><right
    exception on Refactor2.Fail Fail""
    }
  }
