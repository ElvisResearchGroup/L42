{
  type method
  Void (S::ToS that)
    using Alu
      check stringDebug(that.toS().binaryRepr())
      void
  type method
  Void (Library lib)
    using Alu
      check stringDebug(DeployTowel.binaryReprFrom(lib:lib))
      void


  type method
  Void (S::ToS fileName, S::ToS content)
    using Alu
      check fileDebug(
        fileName:fileName.toS().binaryRepr(),
        content:content.toS().binaryRepr()
        )
      void
  type method
  Void (type Any clazz) (
    doc=Introspection(clazz:clazz).record().docOf(path:Path"MyClass")
    Outer0(doc.annotations().top())
    )
  }