{(NestedClass root,Path path, N memberN, N typeN,Record report)<:S::ToS
method toS() S"Type["[this.allAsString()]"]"
method S allAsString() this.report()"AllAsString"  'just the library literal
}

'do mirror for class, then test NestedClass.externalType(), then import methods from design,
' the method referenced path have to parse the string of the first annotation of resolvedPath?