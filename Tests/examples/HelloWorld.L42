{'reuse L42.is/miniBase
  'reuse L42.is/nanoBase0
  reuse L42.is/deployMini

Main:{' do not need to be called main
  Debug(S"Hello world!")
  return ExitCode.normal()
  'return ExitCode.failure()'this is a comment
  }
}