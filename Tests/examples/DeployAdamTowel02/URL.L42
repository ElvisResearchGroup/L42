{ 
  method 
  Void invariant() void 'in order, first declare Reports, that have legit void invariants,
  'then we can use Reports to report invariant violations for other stuff.
  'put a real URL invariant here.
  type method S myName() S"URL"
}