{reuse L42.is/AdamTowel01

// If there is no class binding other than Main,
// and a block comment, then
// otherwise legal code seems to fail strangely.

/* */
  
Main:{
  Debug(S"Execution started.")
  return ExitCode.normal()
  }
}