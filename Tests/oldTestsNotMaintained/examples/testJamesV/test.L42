{reuse L42.is/AdamTowel02


//CompletionBTest:CompletionB.foo(className:N)

Main:{ Debug(S"SKIP")


  s = Strings[S"Stuff";S"And";S"Things"]
  Debug(s.values().toS())

  Debug(S"NO ERRORS")
  return ExitCode.normal()
  }
}

